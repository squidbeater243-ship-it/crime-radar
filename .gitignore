.vercel

# Build output
dist

# Dependencies
node_modules
worker/node_modules

# Cloudflare Workers dev artifacts
worker/.wrangler
.wrangler

# Env / secrets
.env
.env.local
.env.*.local

# OS/editor cruft
.DS_Store
Thumbs.db

# Local agent/skill tooling state (installed via `npx skills add`) --
# skills-lock.json stays tracked so the exact same skills can be
# reinstalled elsewhere; the installed content itself is regenerable,
# like node_modules.
.claude
.agents/skills
